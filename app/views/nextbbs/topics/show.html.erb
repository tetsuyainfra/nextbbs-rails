<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @topic.title %>
</p>
<div>
  <ul>
  <% @comments.each do |c| %>
    <li>
      <%= c.id %> = <%= c.name %> : <%= c.body %>
    </li>
  <% end %>
  </ul>
</div>

<%= render 'nextbbs/comments/form', comment: @new_comment, path: topic_comments_path(@topic) %>

<%= link_to 'Edit', edit_topic_path(@topic) %> |
<%= link_to 'Back', topics_path %>
